#!/usr/bin/env bash
# Build and run the local dev environment (postgres + backend + vite).
set -euo pipefail
cd "$(dirname "$0")/.."

echo "building dx + dx-server..."
go build -o bin/dx       ./cmd/dx
go build -o bin/dx-server ./cmd/dx-server

exec bin/dx serve "$@"
